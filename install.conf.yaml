- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf: etc/tmux.conf

- create:
    - ~/temp-dir

- shell:
  - [git submodule update --init --recursive, Installing submodules]
